spring.application.name=SIU Graduate Project Management

# MongoDB database name (URI is set per profile: dev / prod)
spring.data.mongodb.database=graduate_project

# Jackson
spring.jackson.serialization.write-dates-as-timestamps=false
