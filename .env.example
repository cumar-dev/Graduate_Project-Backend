# Copy this file to .env in the same folder (backend/.env)
# .env is gitignored — never commit real passwords.

# Replace USER, PASSWORD, and cluster0.xxxxx with values from Atlas (Connect → Drivers)
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/graduate_project?retryWrites=true&w=majority
